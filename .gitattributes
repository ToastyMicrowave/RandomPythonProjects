# Normalize all text files to LF in the repo to stop CRLF/LF churn.
* text=auto eol=lf

# Treat known binaries as binary (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.exe binary
*.pyc binary
